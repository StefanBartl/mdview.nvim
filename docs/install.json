{
  "tools": [
    {
      "bin": "curl",
      "required": true,
      "why": "Without curl, mdview.nvim cannot download the relay server binary or client bundle on first `:MDView start`/`:MDView standalone` — health.lua itself treats a missing curl as an error, not a warning, for exactly that reason. Usually already present.",
      "pkg": {
        "apt": "curl",
        "dnf": "curl",
        "pacman": "curl",
        "zypper": "curl",
        "apk": "curl",
        "brew": "curl",
        "winget": "cURL.cURL",
        "scoop": "curl",
        "choco": "curl"
      }
    }
  ]
}
