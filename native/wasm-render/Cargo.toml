[package]
name = "mdview-wasm-render"
version = "0.1.0"
edition = "2021"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
comrak = { version = "0.29", default-features = false }
ammonia = "4"

[profile.release]
opt-level = "s"
lto = true
